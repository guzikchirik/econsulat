package steps.verifications;

import org.testng.Assert;
import pageobjects.EconsulatMainPage;

public class VerificationEconsulatMainPageSteps {

  public void verifyIsMainPageOpen(EconsulatMainPage page) {
//    System.out.println("econsulatMainPage.getEconsulatLink().getReference().toString() = " + page.getEconsulatLink().getReference().toString());
//    Assert.assertTrue(page.getEconsulatLink().isDisplayed());
  }
}
